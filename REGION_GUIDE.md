# 🌍 AWS 리전 선택 가이드

**건양대 6팀 AI 학습 도우미 - 리전 설정 완벽 가이드**

---

## 🚨 중요: 리전이란?

AWS 리전(Region)은 AWS가 데이터 센터를 운영하는 지리적 위치입니다.
예: 서울, 도쿄, 버지니아, 오하이오 등

---

## ⚠️ Bedrock AI 사용 가능 리전

**AI 기능을 사용하려면 반드시 다음 리전 중 하나를 선택해야 합니다:**

| 리전 이름 | 리전 코드 | 추천도 | 설명 |
|-----------|-----------|--------|------|
| **오하이오** | us-east-2 | ⭐⭐⭐ | 문서에 명시된 기본 리전 |
| **버지니아 북부** | us-east-1 | ⭐⭐⭐ | AWS 최대 리전, 가장 안정적 |
| **오레곤** | us-west-2 | ⭐⭐ | 서부 해안 리전 |

---

## ❌ 사용 불가능한 리전

다음 리전들은 **Bedrock을 지원하지 않습니다:**

- ❌ **서울** (ap-northeast-2) - AI 기능 사용 불가!
- ❌ 도쿄 (ap-northeast-1)
- ❌ 싱가포르 (ap-southeast-1)
- ❌ 기타 아시아-태평양 리전

---

## 🎯 권장 리전: 오하이오 (us-east-2)

### 왜 오하이오를 추천하나요?

1. **문서에 명시된 기본 리전**
   ```
   IAM 유저 정보
   리전: 오하이오(us-east-2)
   ```

2. **Bedrock 완전 지원**
   - Claude AI 모델 사용 가능
   - 안정적인 서비스

3. **비용 효율적**
   - 버지니아 북부와 동일한 가격대

---

## 📋 리전 설정 방법

### 방법 1: AWS Console에서 설정 (배포 전)

1. AWS Console 로그인
2. 우측 상단의 리전 이름 클릭
3. **"미국 동부(오하이오) us-east-2"** 선택
4. EC2 인스턴스 생성 진행

### 방법 2: 환경 변수로 설정 (배포 후)

이미 배포했고 리전을 변경하고 싶다면:

```bash
# SSH로 EC2 접속 후

# 환경 변수 파일 생성
sudo tee /etc/environment.d/aws-region.conf > /dev/null <<EOF
AWS_REGION=us-east-2
EOF

# 서비스 재시작
sudo systemctl restart ai-assistant
```

---

## 🔄 리전별 차이점

### 오하이오 (us-east-2) - 권장 ⭐⭐⭐
```
장점:
✅ 문서 기본 리전
✅ Bedrock 완전 지원
✅ 안정적
✅ 중간 레이턴시

단점:
❌ 한국에서 약간 느릴 수 있음 (200-250ms)
```

### 버지니아 북부 (us-east-1) - 권장 ⭐⭐⭐
```
장점:
✅ AWS 최대 리전
✅ 가장 많은 서비스 지원
✅ 매우 안정적
✅ Bedrock 완전 지원

단점:
❌ 한국에서 약간 느릴 수 있음 (200-250ms)
```

### 오레곤 (us-west-2) - 대안 ⭐⭐
```
장점:
✅ Bedrock 지원
✅ 한국에서 상대적으로 가까움

단점:
❌ 서비스 지원이 동부 리전보다 적음
```

### 서울 (ap-northeast-2) - 사용 불가 ❌
```
장점:
✅ 한국에서 가장 빠름 (10-30ms)
✅ 한국어 지원 우수

단점:
❌ Bedrock 미지원 - AI 기능 사용 불가!
❌ 이 프로젝트에는 적합하지 않음
```

---

## 🎓 학교에서 서울 리전을 배운 경우

**질문:** "선생님이 서울 리전을 쓰라고 했는데요?"

**답변:**

일반적인 AWS 실습에서는 서울 리전을 사용하는 것이 맞습니다!
- 빠른 속도
- 한국 사용자 대상

**하지만!** 이 프로젝트는 **AI (Bedrock)**를 사용합니다.

Bedrock은 **미국 리전에서만** 사용 가능합니다.

따라서 이 프로젝트만 예외적으로 **오하이오 또는 버지니아 북부** 리전을 사용해야 합니다.

---

## 🔍 내 EC2가 어느 리전에 있는지 확인하기

### AWS Console에서 확인

1. EC2 대시보드 접속
2. 우측 상단 리전 이름 확인

### SSH로 확인

```bash
# EC2에 접속한 상태에서
curl -s http://169.254.169.254/latest/meta-data/placement/region
```

출력 예시:
```
us-east-2
```

---

## ⚙️ 리전별 애플리케이션 설정

### 프로젝트가 자동으로 리전 감지

우리 애플리케이션(`app.py`)은 다음 순서로 리전을 결정합니다:

1. **환경 변수 `AWS_REGION`** 확인
2. 없으면 **기본값: us-east-2 (오하이오)** 사용

### 리전 변경 방법

#### 영구적으로 변경 (권장)

```bash
# systemd 서비스 파일 수정
sudo systemctl edit ai-assistant

# 다음 내용 추가:
[Service]
Environment="AWS_REGION=us-east-1"

# 저장 후 재시작
sudo systemctl daemon-reload
sudo systemctl restart ai-assistant
```

#### 일시적으로 변경 (테스트용)

```bash
AWS_REGION=us-east-1 python app.py
```

---

## 🧪 리전 테스트

각 리전에서 Bedrock 접근이 가능한지 테스트:

```bash
# Python으로 테스트
python3 << EOF
import boto3

regions = ['us-east-1', 'us-east-2', 'us-west-2', 'ap-northeast-2']

for region in regions:
    try:
        client = boto3.client('bedrock-runtime', region_name=region)
        print(f"✅ {region}: Bedrock 사용 가능")
    except Exception as e:
        print(f"❌ {region}: {str(e)[:50]}...")
EOF
```

---

## 📊 리전별 레이턴시 비교

한국에서 각 리전까지 대략적인 응답 시간:

| 리전 | 레이턴시 | AI 응답 시간 |
|------|----------|--------------|
| 서울 | 10-30ms | ❌ 사용 불가 |
| 오하이오 | 200-250ms | 2-3초 |
| 버지니아 북부 | 200-250ms | 2-3초 |
| 오레곤 | 150-200ms | 2-3초 |

💡 **참고:** AI 응답 시간은 대부분 AI 처리 시간이므로, 리전에 따른 차이는 크지 않습니다.

---

## 🎯 최종 권장사항

### 새로 시작하는 경우
```
리전: 오하이오 (us-east-2)
이유: 문서 기본 리전, 안정적
```

### 이미 버지니아 북부로 시작한 경우
```
리전: 버지니아 북부 (us-east-1)
이유: 그대로 사용해도 문제 없음
```

### 꼭 한국 근처를 원하는 경우
```
리전: 오레곤 (us-west-2)
이유: 상대적으로 가까움
주의: 일부 기능 제한 가능
```

---

## 💡 자주 묻는 질문

### Q: 리전을 나중에 변경할 수 있나요?

A: EC2 인스턴스 자체는 리전 간 이동이 불가능합니다.
하지만 새 리전에서 새 인스턴스를 만들고 다시 배포하면 됩니다. (15분 소요)

### Q: 리전마다 비용이 다른가요?

A: 미국 동부(버지니아, 오하이오)는 비슷한 가격입니다.
오레곤은 약간 더 비쌀 수 있습니다.

### Q: 여러 리전에 동시에 배포할 수 있나요?

A: 가능합니다! 각 리전에 인스턴스를 만들면 됩니다.
하지만 비용이 리전 수만큼 증가합니다.

---

## 🔗 관련 문서

- **AWS Bedrock 리전 지원**: https://docs.aws.amazon.com/bedrock/latest/userguide/bedrock-regions.html
- **AWS 리전 목록**: https://aws.amazon.com/about-aws/global-infrastructure/regions_az/

---

## 📞 도움이 필요하신가요?

리전 설정이 헷갈리면:
1. **기본 선택: 오하이오 (us-east-2)**
2. 999 채널 또는 @NxtCloud_김유림에게 문의

---

**건양대학교 AI 부트캠프 6팀**

Happy Cloud Computing! ☁️
